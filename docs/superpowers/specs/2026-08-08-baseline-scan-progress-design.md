# 基准同步：规则自动匹配与扫描进度

日期：2026-08-08  
状态：已批准并实现

## 自动匹配

选中基准库后，按各规则 `pattern` 反匹配库名；多命中时按字面量长度与取值列表吻合度打分选最佳。可手动改规则。

## 扫描进度 / 终止

- 前端传入 `job_id`，Rust 按步 `emit("baseline-scan-progress")`
- `cancel_baseline_scan(job_id)` 置取消标志；下一步前停止，不写入 scan 缓存
- UI：进度条 + 「终止扫描」
